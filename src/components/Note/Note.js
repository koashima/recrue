import React from 'react'
import { Form, TextArea } from 'semantic-ui-react'

const Note = (props) => { 

  return(
    <Form>
      <TextArea placeholder='PROSPECT NOTES' />
    </Form>
  )
}

export default Note;